# 토스 NEXT ML Challenge CTR 모델 프로젝트 필수 라이브러리
# Python 3.11.9 호환

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0  # parquet 파일 처리

# 머신러닝 프레임워크
scikit-learn>=1.3.0
lightgbm>=4.0.0
xgboost>=1.7.0
catboost>=1.2.0

# 딥러닝 (PyTorch)
torch>=2.0.0
torchvision>=0.15.0

# 하이퍼파라미터 최적화
optuna>=3.3.0

# 평가 및 시각화
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# 유틸리티
tqdm>=4.65.0
joblib>=1.3.0
psutil>=5.9.0  # 시스템 모니터링

# 로깅 및 설정
pyyaml>=6.0
configparser>=5.3.0

# 웹 API (선택사항)
flask>=2.3.0
fastapi>=0.100.0
uvicorn>=0.23.0

# 테스트
pytest>=7.4.0
pytest-cov>=4.1.0

# 개발 도구
black>=23.7.0  # 코드 포맷팅
flake8>=6.0.0  # 린팅
jupyter>=1.0.0  # 노트북

# 메모리 최적화
pympler>=0.9  # 메모리 프로파일링

# 타입 힌트
typing-extensions>=4.7.0

# 날짜/시간 처리
python-dateutil>=2.8.0

# 수치 계산 최적화
numba>=0.57.0  # JIT 컴파일

# 통계 분석
scipy>=1.11.0
statsmodels>=0.14.0

# 진행률 표시
rich>=13.4.0

# 병렬 처리
multiprocessing-logging>=0.3.4

# 캐싱
diskcache>=5.6.0

# 네트워크 요청 (API 관련)
requests>=2.31.0

# 환경 변수 관리
python-dotenv>=1.0.0

# 파일 시스템 감시
watchdog>=3.0.0

# 압축
lz4>=4.3.0  # 빠른 압축

# JSON 처리
orjson>=3.9.0  # 빠른 JSON 파싱

# 메모리 효율적 데이터 구조
pyroaring>=0.4.0  # 비트맵

# 프로파일링
line_profiler>=4.1.0
memory_profiler>=0.61.0

# 실험 추적 (선택사항)
wandb>=0.15.0
mlflow>=2.5.0

# 모델 해석
shap>=0.42.0  # 모델 해석

# 시계열 처리 (광고 데이터의 시간 패턴 분석용)
pandas-ta>=0.3.14b

# 고급 전처리
category_encoders>=2.6.0
feature-engine>=1.6.0

# GPU 가속 (선택사항 - CUDA 환경)
# cudf>=23.08.0  # GPU 데이터프레임
# cuml>=23.08.0  # GPU 머신러닝

# 보안
cryptography>=41.0.0

# 로깅 확장
colorlog>=6.7.0  # 컬러 로깅

# 설정 검증
pydantic>=2.0.0